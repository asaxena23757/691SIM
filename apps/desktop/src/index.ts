// Desktop application entry point.
// The real Tauri + React Flow editor is built out in Phase 6.
// This placeholder just proves the workspace wires together end to end.

import { HardwareModel } from '@691sim/core';

console.log('691SIM desktop app placeholder loaded.', HardwareModel);
